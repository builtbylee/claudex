interface:
  display_name: "Claudex Plan Review"
  short_description: "Safe second-opinion plan review"
  default_prompt: "Use $codex-plan-review to get a safe Claudex second opinion on a plan file."
policy:
  allow_implicit_invocation: true
